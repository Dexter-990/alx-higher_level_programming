#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    t_a = tuple_a
    t_b = tuple_b
    if len(t_a) < 2:
        if t_a[0] == None:
            t_a[0] = 0
        elif t_a[1] == None:
            t_a[1] == 0
    elif len
    a = t_a[0] + t_b[0]
    b = t_a[1] + t_b[1]
    return (a, b)
